# Example seeds

Intentionally empty. Real seed runs are created with `init-game-run.mjs` (run state is
gitignored on disk). Handoff truth for a completed run lives in that run's
`execution-ledger.jsonl` and `manifest.spec_pack_path`.