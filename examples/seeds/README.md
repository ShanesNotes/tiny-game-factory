# Example seeds

Intentionally empty. Real seed runs are created with `init-game-run.mjs` (run state is
gitignored on disk). For a completed end-to-end walkthrough see
`docs/handoffs/dolphin-tgf-e2e-RESULT.md`.